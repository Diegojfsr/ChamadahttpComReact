
// Exemplo 2
// É possível atribuir a uma variável o resultado de uma expressão:

var totalBruto = 5000
var desconto = 0.1

var totalLiquido = totalBruto - (totalBruto * desconto)


console.log(totalLiquido);